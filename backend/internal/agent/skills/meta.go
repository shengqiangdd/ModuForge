package skills

import (
	"github.com/moduforge/backend/internal/agent/registry"
)

// Re-export registry types for backward compatibility.
type SkillMeta = registry.SkillMeta
type MetadataProvider = registry.MetadataProvider
