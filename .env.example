# ModuForge 环境变量配置示例
# 复制此文件为 .env 并填入实际值

# JWT 签名密钥
# - 显式设置：使用该值
# - 留空/不设：首次启动自动生成并保存到持久化卷 /data/.env，重启后自动读取同一密钥
# 生成方法: openssl rand -hex 32
JWT_SECRET=

# 数据库路径
DB_PATH=/data/moduforge.db

# 服务端口
PORT=8080

# 运行模式: release / debug
GIN_MODE=release
